name: Update Game Data

on:
  # schedule:
  #   # Run daily at 6 AM UTC
  #   - cron: '0 6 * * *'
  workflow_dispatch: # Allow manual trigger

permissions:
  contents: write
  actions: write

jobs:
  update-data:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Fetch latest game data
        run: npm run fetch-data

      - name: Validate data
        run: npm run validate-data

      - name: Check for changes
        id: git-check
        run: |
          git diff --exit-code public/data || echo "changed=true" >> $GITHUB_OUTPUT

      - name: Commit and push if changed
        if: steps.git-check.outputs.changed == 'true'
        run: |
          git config user.name "GitHub Actions Bot"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add public/data public/assets/icons
          git commit -m "🤖 Auto-update: Game data updated"
          git push

      - name: Trigger deploy workflow
        if: steps.git-check.outputs.changed == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh workflow run deploy.yml

      - name: Create changelog
        if: steps.git-check.outputs.changed == 'true'
        run: |
          echo "Game data was updated on $(date -u +'%Y-%m-%d %H:%M UTC')" >> $GITHUB_STEP_SUMMARY
          echo "Source: MetaForge API (metaforge.app/arc-raiders)" >> $GITHUB_STEP_SUMMARY
